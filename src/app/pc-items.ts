export interface EmployeesInterface {
  length: number;
  // eslint-disable-next-line @typescript-eslint/naming-convention
  userid: string;
  // eslint-disable-next-line @typescript-eslint/naming-convention
  employeeid: string;
  // eslint-disable-next-line @typescript-eslint/naming-convention
  employeename: string;
  // eslint-disable-next-line @typescript-eslint/naming-convention
  project_id: string;
}
