export interface RmdInt {
  // eslint-disable-next-line @typescript-eslint/naming-convention
  rm_userid: string;
  // eslint-disable-next-line @typescript-eslint/naming-convention
  rm_employeeid: string;
  // eslint-disable-next-line @typescript-eslint/naming-convention
  rm_name: string;
}
