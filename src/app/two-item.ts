export interface JsonTWO {
  // eslint-disable-next-line @typescript-eslint/naming-convention
  UserId: string;
  // eslint-disable-next-line @typescript-eslint/naming-convention
  EmployeeId: string;
  // eslint-disable-next-line @typescript-eslint/naming-convention
  EmpRole: string;
  // eslint-disable-next-line @typescript-eslint/naming-convention
  EmployeeName: string;
  // eslint-disable-next-line @typescript-eslint/naming-convention
  CompanyEmail: string;
  // eslint-disable-next-line @typescript-eslint/naming-convention
  ContactNumber: string;
  // eslint-disable-next-line @typescript-eslint/naming-convention
  ReportingManager: string;
  // eslint-disable-next-line @typescript-eslint/naming-convention
  ProfileImg: string;
  // eslint-disable-next-line @typescript-eslint/naming-convention
  EmpRMData: string;
  // eslint-disable-next-line @typescript-eslint/naming-convention
  AssignedProjects: [];
  // eslint-disable-next-line @typescript-eslint/naming-convention
  EmpAsRMEmployees: [];
  // eslint-disable-next-line @typescript-eslint/naming-convention
  Employees: [];
}
